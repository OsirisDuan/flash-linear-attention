# 目标

请系统检查并补充这个仓所有kernel函数的测试用例情况（如确实没有只提供没有测试用例的信息即可，不用补充用例），再次刷新报告文档kernel_analysis_report.md。这是一个很大的任务，因此本文档先给出总体任务，然后给出切分后的子任务，最后给出已经完成的子任务。

# 总体任务

1. 全面梳理仓库中所有算子，创建完整清单，精确包含以下信息：

- 算子标准名称（需与官方定义一致）
- 算子所属的功能模块/分类
- 算子核心功能的专业描述（不超过50字）
- 明确列出您之前搜索到的所有kernel函数与对应算子的映射关系，包括一对一、一对多、多对一的关联情况

2. 为每个kernel函数匹配对应的测试用例详细信息：

- 测试用例文件的完整相对路径（从项目根目录开始）
- 测试函数的精确名称（包含命名空间和类名，如适用）
- 测试用例的类型（单元测试/集成测试/性能测试）

3. 针对特殊测试场景进行明确标注：

- 多kernel合并为一个算子共享测试用例时，需完整列出该测试用例所覆盖的全部算子名称及对应kernel函数
- 多个独立算子共享同一测试用例时，需清晰指明所有相关算子及kernel函数，并说明共享原因

4. 对于确实没有单独测试用例的算子，必须在对应字段中明确标注"无测试用例"状态，并简要说明可能原因（如：暂未实现/已在集成测试中覆盖/计划开发中）
5. 最终整理结果需以规范化表格形式呈现，表格必须包含以下列：

- 算子名称（主列，可合并相同算子行）
- 算子下属kernel函数（每行一个kernel）
- 所属模块
- 功能描述
- 测试用例路径
- 测试用例覆盖情况（单选：单个算子/多个算子/无测试用例）
- 备注（可选，用于说明特殊情况）

表格应按模块分类排序，同一模块内按算子名称字母顺序排列，确保信息清晰易读，便于审计和后续测试工作开展。

# 子任务

针对上述任务，分出具体的子任务如下：
子任务1：建立算子清单与kernel映射关系
- **输入件**：无（从代码库中实时提取）
- **输出件**：`tmp/01_operator_kernel_mapping.json`
- **工作内容**：
  - 扫描所有`fla/ops/*/`目录下的`__init__.py`文件，提取算子标准名称
  - 扫描所有包含`@triton.jit`的源文件，提取kernel函数名
  - 建立算子→kernel的映射关系（一对一/一对多/多对一）
  - 记录每个算子的所属模块和功能描述

子任务2：收集测试用例信息
- **输入件**：无（从tests/目录中提取）
- **输出件**：`tmp/02_test_cases.json`
- **工作内容**：
  - 扫描`tests/ops/`目录下的所有测试文件
  - 提取每个测试函数的名称、文件路径
  - 分析测试函数的导入依赖，确定其测试的目标算子
  - 标注测试类型（单元测试/集成测试/性能测试）

子任务3：建立kernel与测试用例的映射关系
- **输入件**：`tmp/01_operator_kernel_mapping.json`、`tmp/02_test_cases.json`
- **输出件**：`tmp/03_kernel_test_mapping.json`
- **工作内容**：
  - 根据测试用例的导入依赖，匹配对应的算子
  - 根据算子→kernel映射，推导kernel→测试用例关系
  - 识别多kernel共享测试用例的情况
  - 识别多算子共享测试用例的情况
  - 标注无测试用例的kernel

子任务4：生成规范化表格
- **输入件**：`tmp/01_operator_kernel_mapping.json`、`tmp/03_kernel_test_mapping.json`
- **输出件**：`tmp/04_kernel_test_table.md`
- **工作内容**：
  - 按模块分类排序
  - 同一模块内按算子名称字母顺序排列
  - 生成包含以下列的markdown表格：
    - 算子名称
    - 算子下属kernel函数
    - 所属模块
    - 功能描述
    - 测试用例路径
    - 测试用例覆盖情况
    - 备注

子任务5：更新报告文档
- **输入件**：`tmp/04_kernel_test_table.md`
- **输出件**：更新`kernel_analysis_report.md`
- **工作内容**：
  - 将生成的表格追加到报告文档中
  - 添加统计摘要信息
  - 确保文档格式完整

输出件汇总

| 子任务 | 输出文件 | 内容说明 |
|--------|----------|----------|
| 1 | `tmp/01_operator_kernel_mapping.json` | 算子与kernel映射关系 |
| 2 | `tmp/02_test_cases.json` | 测试用例清单 |
| 3 | `tmp/03_kernel_test_mapping.json` | kernel与测试用例映射 |
| 4 | `tmp/04_kernel_test_table.md` | 规范化表格 |
| 5 | `kernel_analysis_report.md` | 最终报告（更新） |

# 子任务完成情况

- 子任务1：已完成
- 子任务2：未完成
- 子任务3：未完成
- 子任务4：未完成
- 子任务5：未完成