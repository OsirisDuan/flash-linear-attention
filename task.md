# 目标

请系统检查并补充这个仓所有kernel函数的测试用例情况（如确实没有只提供没有测试用例的信息即可，不用补充用例），再次刷新报告文档kernel_analysis_report.md。这是一个很大的任务，因此本文档先给出总体任务，然后给出切分成多个子任务。注意，你的所有中间产物文档都必须放在repo的根目录下的tmp文件夹下。

# 环境信息

你现在暂时还没有python环境，所以无法运行python脚本。

# 总体任务

1. 全面梳理仓库中所有算子，创建完整清单，精确包含以下信息：

- 算子标准名称（需与官方定义一致）
- 算子所属的功能模块/分类
- 算子核心功能的专业描述（不超过50字）
- 明确列出您之前搜索到的所有kernel函数与对应算子的映射关系，包括一对一、一对多、多对一的关联情况

2. 为每个kernel函数匹配对应的测试用例详细信息：

- 测试用例文件的完整相对路径（从项目根目录开始）
- 测试函数的精确名称（包含命名空间和类名，如适用）
- 测试用例的类型（单元测试/集成测试/性能测试）

3. 针对特殊测试场景进行明确标注：

- 多kernel合并为一个算子共享测试用例时，需完整列出该测试用例所覆盖的全部算子名称及对应kernel函数
- 多个独立算子共享同一测试用例时，需清晰指明所有相关算子及kernel函数，并说明共享原因

4. 对于确实没有单独测试用例的算子，必须在对应字段中明确标注"无测试用例"状态，并简要说明可能原因（如：暂未实现/已在集成测试中覆盖/计划开发中）

5. 最终整理结果到kernel_analysis_report.md，需以规范化表格形式呈现，表格必须包含以下列：

- 算子名称（主列，可合并相同算子行）
- 算子下属kernel函数（每行一个kernel）
- 所属模块
- 功能描述
- 测试用例路径
- 测试用例覆盖情况（单选：单个算子/多个算子/无测试用例）
- 备注（可选，用于说明特殊情况）

表格应按模块分类排序，同一模块内按算子名称字母顺序排列，确保信息清晰易读，便于审计和后续测试工作开展。

---

# 子任务拆分方案

根据任务的复杂度和工作量，将总体任务拆分为以下5个子任务：

## 子任务1：梳理算子清单和kernel函数映射关系

**任务目标**：全面梳理仓库中所有算子，建立算子与kernel函数的完整映射关系

**输入件**：
- fla/ops/ 目录下的所有算子源码
- fla/modules/ 目录下的所有模块源码
- fla/layers/ 目录下的所有层源码

**输出件**：
- tmp/operators_kernels_mapping.csv（算子与kernel函数映射表）

**工作内容**：
1. 扫描 fla/ops/ 目录下的所有Python文件
2. 提取所有 @triton.jit 装饰的 kernel 函数
3. 分析每个算子的实现文件，识别算子名称、所属模块、功能描述
4. 建立算子与 kernel 函数的映射关系（一对一、一对多、多对一）
5. 记录每个 kernel 函数的源码路径和行号

**CSV表格格式**：
```
算子名称,算子所属模块,算子功能描述,Kernel函数名称,Kernel源码路径,Kernel行号,映射关系类型
```

**预估工作量**：中等（需要扫描大量源码文件，但主要是自动化提取）

---

## 子任务2：梳理测试用例清单

**任务目标**：梳理仓库中针对算子和单kernel函数的测试用例，建立测试用例清单

**输入件**：
- tests/ops/ 目录下的所有算子测试文件
- tests/modules/ 目录下的所有模块测试文件
- tests/context_parallel/ 目录下的测试文件（如有）
- 注意：排除 tests/models/ 目录下的模型级别测试

**输出件**：
- tmp/test_cases_inventory.csv（测试用例清单）

**工作内容**：
1. 扫描 tests/ops/ 目录下的所有算子测试文件
2. 扫描 tests/modules/ 目录下的所有模块测试文件
3. 提取每个测试文件中的测试函数名称
4. 识别测试类型（单元测试/集成测试/性能测试）
5. 分析测试函数的测试对象（测试哪个算子或kernel函数）
6. 记录测试文件的完整相对路径
7. 严格排除模型级别的测试代码（如 tests/models/ 目录）

**CSV表格格式**：
```
测试文件路径,测试函数名称,测试类型,测试对象,测试对象类型,备注
```

**预估工作量**：中等（需要分析测试文件结构）

---

## 子任务3：匹配kernel函数与测试用例

**任务目标**：建立kernel函数与测试用例的对应关系，分析测试覆盖情况

**输入件**：
- tmp/operators_kernels_mapping.csv（子任务1输出）
- tmp/test_cases_inventory.csv（子任务2输出）
- tests/ 目录下的测试源码

**输出件**：
- tmp/kernel_test_coverage.csv（kernel测试覆盖情况表）

**工作内容**：
1. 分析测试函数的调用链，识别测试了哪些算子
2. 通过算子与kernel的映射关系，推导测试覆盖的kernel函数
3. 标注每个kernel函数的测试覆盖情况
4. 对于没有测试用例的kernel，标注"无测试用例"状态
5. 简要说明无测试用例的可能原因

**CSV表格格式**：
```
Kernel函数名称,所属算子,测试用例路径,测试函数名称,测试覆盖情况,无测试用例原因,备注
```

**预估工作量**：较大（需要深入分析测试代码和调用关系）

---

## 子任务4：分析特殊测试场景

**任务目标**：识别和分析特殊测试场景，如多kernel合并测试、多算子共享测试等

**输入件**：
- tmp/kernel_test_coverage.csv（子任务3输出）
- tests/ 目录下的测试源码

**输出件**：
- tmp/special_test_scenarios.csv（特殊测试场景分析）

**工作内容**：
1. 识别多kernel合并为一个算子共享测试用例的情况
2. 识别多个独立算子共享同一测试用例的情况
3. 分析共享测试用例的原因
4. 完整列出共享测试用例所覆盖的全部算子名称及对应kernel函数
5. 标注特殊测试场景的类型和说明

**CSV表格格式**：
```
测试用例路径,测试函数名称,特殊场景类型,覆盖的算子列表,覆盖的Kernel列表,共享原因,备注
```

**预估工作量**：中等（需要分析测试代码的特殊模式）

---

## 子任务5：生成最终报告

**任务目标**：整合所有信息，生成规范化的最终报告文档

**输入件**：
- tmp/operators_kernels_mapping.csv（子任务1输出）
- tmp/test_cases_inventory.csv（子任务2输出）
- tmp/kernel_test_coverage.csv（子任务3输出）
- tmp/special_test_scenarios.csv（子任务4输出）

**输出件**：
- kernel_analysis_report.md（更新后的最终报告）

**工作内容**：
1. 整合所有CSV文件的信息
2. 按照要求的表格格式生成规范化报告
3. 表格按模块分类排序，同一模块内按算子名称字母顺序排列
4. 包含所有必需列：算子名称、算子下属kernel函数、Kernel定义路径、所属模块、功能描述、测试用例路径、测试用例覆盖情况、备注
5. 添加统计总结和关键发现
6. 更新报告生成时间

**表格格式要求**：
```
算子名称,算子下属kernel函数,Kernel定义路径,所属模块,功能描述,测试用例路径,测试用例覆盖情况,备注
```

**预估工作量**：中等（主要是数据整合和格式化）

---

## 子任务执行顺序

建议按照以下顺序执行子任务：

1. **子任务1** → 2. **子任务2** → 3. **子任务3** → 4. **子任务4** → 5. **子任务5**

每个子任务完成后，需要验证输出件的完整性和正确性，再进行下一个子任务。

---

## 注意事项

1. 所有中间产物文档必须放在 repo 的根目录下的 tmp 文件夹下
2. 记录信息时使用 CSV 表格形式，不使用 JSON 格式
3. 暂时没有 Python 环境，无法运行 Python 脚本，所有分析工作需要通过代码阅读和静态分析完成
4. 每个子任务完成后需要验证输出件的正确性
5. 最终报告需要符合任务要求的表格格式和排序规则